const fs = require('fs');

async function run() {
    try {
        const formData = new FormData();
        // Make a tiny fake 1-page PDF so it passes validation and `extractFromPdf` parses it.
        // wait, I don't have a valid PDF. Let's just create one.
    } catch(e) {}
}
